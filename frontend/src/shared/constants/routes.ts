export const APP_ROUTES = {
  landing: "/",
  login: "/auth/login",
  register: "/auth/register",
  editor: "/editor",
  settings: "/settings",
  history: "/history",
  presets: "/presets",
} as const;
