# LuminaStudio Web Workflow

Before editing, read `AGENTS.md`, confirm intent from `docs/tdd/ls-web-tdd.md`, and inspect exact source files.

Use `.agents/skills/` for repeatable workflows. Use Graphify/repo maps/summaries only to find files, not as implementation truth.

Run relevant verification (`npm run lint`, `npm run typecheck`, `npm run test`, `npm run build`) before claiming success.
