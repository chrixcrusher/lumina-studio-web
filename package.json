{
  "name": "lumina-studio-web",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "build": "npm run build --workspace frontend && npm run build --workspace backend",
    "lint": "npm run lint --workspace frontend && npm run lint --workspace backend",
    "typecheck": "npm run typecheck --workspace frontend && npm run typecheck --workspace backend",
    "test": "npm run test --workspace frontend && npm run test --workspace backend",
    "verify": "npm run lint && npm run typecheck && npm run test && npm run build",
    "smoke:deploy": "node scripts/deployment-smoke.mjs",
    "ai:check": "npm run verify",
    "ai:index": "node scripts/generate-ai-index.mjs",
    "ai:context": "npm run ai:index",
    "ai:graph": "graphify update . --no-description --no-label"
  },
  "devDependencies": {
    "@juliusbrussee/caveman-code": "^0.65.2",
    "@sentropic/graphify": "^0.14.0",
    "jsdom": "^25.0.1",
    "likecu-understand-anything": "^1.0.0"
  }
}
